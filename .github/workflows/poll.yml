name: Poll Influencers and Run Evaluator

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  poll_and_evaluate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install feedparser requests pandas

                - name: Test Slack connection
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
        run: |
          PAYLOAD="{\"text\": \"🔔 Test ping from ProjectX_Notifier to #business-falauda\"}"
          curl -X POST -H 'Content-type: application/json' --data "$PAYLOAD" $SLACK_WEBHOOK


      - name: Run fetchers
        env:
          PYTHONPATH: ${{ github.workspace }}
          X_BEARER: ${{ secrets.X_BEARER }}
          NEWSAPI_KEY: ${{ secrets.NEWSAPI_KEY }}
        run: |
          python src/fetchers/rss_fetcher.py
          python src/fetchers/newsapi_fetcher.py
